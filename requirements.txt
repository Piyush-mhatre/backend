fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- stock analysis feature ---
yfinance==1.7.0
pandas==2.2.3
numpy==1.26.4
plotly==5.24.1
requests==2.32.3

# Prophet is listed so the FORECAST_ENGINE=prophet switch works without a
# redeploy — but it is only ever imported if that env var is set, so it
# costs disk/build time, not runtime RAM, while FORECAST_ENGINE=trend
# (the default) is active.
prophet==1.4.0

# --- news sentiment feature ---
# NOTE: torch is intentionally NOT listed here anymore. Inference now
# runs on onnxruntime, which imports in a small fraction of torch's
# memory footprint (torch's own runtime — libtorch/MKL/OpenMP — costs
# 200-300MB just by existing in the process, before any model is even
# loaded, which is what was causing repeated OOM crashes on Render's
# 512MB free tier even after every quantization-level fix).
#
# transformers is kept ONLY for AutoTokenizer — tokenizer classes do not
# require torch, tensorflow, or any other backend framework to be
# installed; they work standalone.
transformers==4.49.0
onnxruntime==1.20.1

# --- gold price + AI insights feature ---
# Google's current unified GenAI SDK (the old google-generativeai
# package is deprecated). Lightweight HTTP client — no torch/transformers-
# scale memory footprint, but still lazy-imported in gold.py to keep
# app-boot memory minimal regardless.
google-genai